# Use mock analyze (1.5s delay, no ML server). Set to false when your FastAPI service is ready.
NEXT_PUBLIC_USE_MOCK=true

# When NEXT_PUBLIC_USE_MOCK=false, requests are forwarded here (POST multipart field "image").
# Example: https://your-ml-service.railway.app
ML_API_URL=
