# true = browser mock (see src/lib/mock.ts). false = call real ML from src/lib/api.ts only.
NEXT_PUBLIC_USE_MOCK=true

# Base URL of your ML service (no trailing slash required). POST goes to {URL}/api/analyze with multipart field "image".
NEXT_PUBLIC_ML_API_URL=
